{
  "name": "ts-contract-example",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "proto:gen": "pnpm buf generate . --template ./buf.gen.yaml",
    "proto:clean": "rimraf gen"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.61.0",
    "rimraf": "6.1.2"
  }
}
